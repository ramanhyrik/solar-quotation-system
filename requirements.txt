fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
jinja2==3.1.3
reportlab==4.0.9
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
pillow==10.2.0
python-bidi==0.4.2
arabic-reshaper==3.0.0
requests==2.31.0
sendgrid==6.11.0
numpy==1.26.4
shapely==2.0.2
matplotlib==3.8.2
psycopg2-binary==2.9.9
# SAM 3 API - Uses HuggingFace Space instead of local model
# No need for torch/transformers locally - we call the API via requests

# PHASE 1: Map Integration & Geocoding (FREE tier)
# - Nominatim API (OpenStreetMap) for geocoding - FREE, no API key required
# - Mapbox Static API for satellite imagery - 100k requests/month FREE (requires MAPBOX_ACCESS_TOKEN)
